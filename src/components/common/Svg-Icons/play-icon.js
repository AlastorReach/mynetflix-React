import React from 'react';

const PlayIcon = () => (
    <svg className="svg-icon svg-icon-nfplayerPlay" focusable="false">
        <use filter="" xlinkHref="#nfplayerPlay"></use>
    </svg>
);

export default PlayIcon;